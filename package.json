{
  "name": "multi-tenant-ai-platform",
  "private": true,
  "packageManager": "pnpm@8.8.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.14.0",
    "eslint": "^8.57.0",
    "prettier": "^3.0.0",
    "turbo": "^1.12.8",
    "typescript": "^5.6.0"
  }
}
